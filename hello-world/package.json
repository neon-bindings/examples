{
  "name": "threading-hint",
  "version": "0.1.0",
  "description": "Returns a number indicating how much hardware multithreading the current machine supports.",
  "main": "lib/index.js",
  "dependencies": {
    "neon-cli": "^0.2.0"
  },
  "scripts": {
    "install": "neon build --release"
  }
}
