{
  "name": "workspace",
  "version": "0.1.0",
  "description": "Example of neon in a workspace",
  "main": "lib/index.js",
  "dependencies": {
    "neon-cli": "^0.3.3"
  },
  "scripts": {
    "install": "neon build",
    "version": "neon build --release"
  },
  "devDependencies": {
    "eslint": "^6.8.0"
  }
}
