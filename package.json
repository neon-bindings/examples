{
  "name": "examples",
  "version": "1.0.0",
  "description": "Examples for Neon",
  "main": "index.js",
  "scripts": {
    "lint": "eslint .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "eslint": "^5.8.0",
    "eslint-config-bliss": "^3.0.0",
    "lerna": "^3.4.3"
  },
  "eslintConfig": {
    "extends": "bliss",
    "rules": {
      "no-plusplus": "off"
    }
  }
}
