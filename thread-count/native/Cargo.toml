[package]
name = "thread-count"
version = "0.1.0"
build = "build.rs"
edition = "2018"

[lib]
name = "thread_count"
crate-type = ["cdylib"]

[build-dependencies]
neon-build = "0.3.3"

[dependencies]
neon = "0.3.3"
num_cpus = "1.4.0"
