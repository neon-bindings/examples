{
  "name": "electron-app",
  "version": "0.1.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "install": "electron-build-env neon build @amilajack/neon-hello --release",
    "version": "electron-build-env neon build @amilajack/neon-hello --release"
  },
  "devDependencies": {
    "electron-build-env": "^0.2.0",
    "eslint": "^6.8.0",
    "neon-cli": "^0.3.3"
  },
  "dependencies": {
    "@amilajack/neon-hello": "^1.0.1",
    "electron": "^7.1.11"
  },
  "eslintConfig": {
    "rules": {
      "import/no-unresolved": "off"
    }
  }
}
