{
  "name": "electron-app",
  "version": "0.1.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-build-env neon build hello-world",
    "postinstall": "npm run build"
  },
  "devDependencies": {
    "electron-build-env": "^0.2.0",
    "neon-cli": "^0.3.3"
  },
  "dependencies": {
    "electron": "^7.1.11",
    "hello-world": "0.1.0"
  },
  "eslintConfig": {
    "rules": {
      "import/no-unresolved": "off"
    }
  }
}
