{
  "name": "electron-app",
  "version": "0.1.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-build-env neon build @amilajack/neon-hello --release",
    "postinstall": "npm run build"
  },
  "devDependencies": {
    "electron-build-env": "^0.2",
    "neon-cli": "^0.3.1"
  },
  "dependencies": {
    "electron": "3.0.10",
    "@amilajack/neon-hello": "^0.3.1"
  },
  "eslintConfig": {
    "rules": {
      "import/no-unresolved": "off"
    }
  }
}
