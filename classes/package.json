{
  "name": "classes",
  "version": "0.0.1",
  "main": "lib/index.js",
  "dependencies": {
    "neon-cli": "^0.3.3"
  },
  "scripts": {
    "install": "neon build",
    "version": "neon build --release"
  },
  "devDependencies": {
    "eslint": "^6.8.0"
  }
}
